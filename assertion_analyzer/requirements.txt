# Assertion Analyzer Dependencies
# Install with: pip install -r requirements.txt

# Microsoft Authentication Library with broker support for Windows SSO
msal[broker]>=1.24.0

# HTTP requests for API calls
requests>=2.28.0
